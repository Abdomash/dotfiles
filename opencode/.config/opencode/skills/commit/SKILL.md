---
name: commit
description: Instructions for git commits
---

Commit the specified code changes. Ensure large changes are split into multiple, composable, logically-separated commits. Format commits as `<type>(<scope>): <description>` or what convention you see in `git log --oneline` of the repo.
